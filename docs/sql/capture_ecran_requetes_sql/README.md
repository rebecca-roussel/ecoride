# Captures d’écran — Requêtes SQL

Ce dossier contient les captures d’écran des requêtes SQL exécutées (vérification du schéma, des données et des contraintes).

Les scripts correspondants sont dans `../`.
